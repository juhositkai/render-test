{
  "name": "notebackend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "build:ui": "rm -rf dist && cd ../frontend && npm run dist && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "lint": "eslint ."
  },
  "author": "Juho Ropanen",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "mongoose": "^8.7.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.11.0",
    "@stylistic/eslint-plugin-js": "^2.8.0",
    "cross-env": "^7.0.3",
    "eslint": "^9.11.0",
    "globals": "^15.9.0",
    "nodemon": "^3.0.1",
    "supertest": "^7.0.0"
  }
}
